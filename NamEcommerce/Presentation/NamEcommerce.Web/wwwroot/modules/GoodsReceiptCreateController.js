import ProductPicker from "/modules/ProductPicker.js";import ProductBrowser from "/modules/ProductBrowser.js";import { getWarehouseSettings } from "/modules/Settings.js";// â”€â”€â”€ Controller â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€export default class GoodsReceiptCreateController {    #state = {};    #productPicker = null;    #browser = null;    #warehouseSettings;    #warehouseOptions;    constructor() {        this.#warehouseSettings = getWarehouseSettings();        this.#bindModal();        const warehouseSelect = document.getElementById('WarehouseId');        warehouseSelect.addEventListener('change', (e) => {            this.#setState({ warehouse: e.target.value });        });        const warehouse = warehouseSelect.value;        this.#bindTableEvents();        const browserEl = document.getElementById('productBrowser');        if (browserEl) {            this.#browser = new ProductBrowser(                browserEl,                (product) => this.#addOrIncrementItem(product),                { purchase: true }            );            this.#browser.init();        }        this.#setState({ warehouse });        this.#warehouseOptions = Array.from(warehouseSelect.querySelectorAll('option')).filter(option => option.value)            .map(option => ({ value: option.value, label: option.label }));    }    #setState(patch) {        this.#state = Object.assign({}, this.#state, patch);    }    #bindModal() {        const pickerEl = document.getElementById('productPicker');        if (!pickerEl) return;        this.#productPicker = new ProductPicker(pickerEl, { purchase: true });        pickerEl.addEventListener('select', (e) => {            const product = e.detail?.product;            if (product) {                document.getElementById('modalProductId').value = product.id;                pickerEl.dataset.selectedName = product.name;                pickerEl.dataset.selectedPicture = product.picture ?? '';                document.getElementById('modalProductDetails')?.classList.remove('d-none');                document.getElementById('btnAddItemConfirm')?.classList.remove('d-none');                document.getElementById('productPickerError')?.classList.add('d-none');            }        });        pickerEl.addEventListener('remove', () => {            document.getElementById('modalProductId').value = '';            document.getElementById('modalProductDetails')?.classList.add('d-none');            document.getElementById('btnAddItemConfirm')?.classList.add('d-none');        });        document.getElementById('modalWarehouseId')?.addEventListener('change', (e) => {            if (!this.#warehouseSettings.AllowNonWarehouse && !e.target.value) {                document.getElementById('modalWarehouseError')?.classList.remove('d-none');                return;            }            document.getElementById('modalWarehouseError')?.classList.add('d-none');        });        document.getElementById('btnAddItemConfirm')?.addEventListener('click', () => {            const productId = document.getElementById('modalProductId')?.value;            const productName = pickerEl.dataset.selectedName ?? 'â€”';            const productPicture = pickerEl.dataset.selectedPicture ?? '';            const warehouseId = document.getElementById('modalWarehouseId')?.value ?? '';            const qtyRaw = document.getElementById('modalQty')?.value ?? '0';            const costRaw = document.getElementById('modalUnitCost')?.value ?? '';            if (!productId) {                document.getElementById('productPickerError')?.classList.remove('d-none');                return;            }            const qty = parseFloat(DecimalFields.stripFormatting(qtyRaw, 2));            if (!qty || qty <= 0) {                document.getElementById('modalQtyError')?.classList.remove('d-none');                return;            }            if (!this.#warehouseSettings.AllowNonWarehouse && !warehouseId) {                document.getElementById('modalWarehouseError')?.classList.remove('d-none');                return;            }            const cost = costRaw ? (parseFloat(DecimalFields.stripFormatting(costRaw)) || null) : null;            if (cost < 0) {                document.getElementById('modalUnitCostError')?.classList.remove('d-none');                return;            }            this.#addItemToTable(                { id: productId, name: productName, picture: productPicture },                qty, cost, warehouseId            );            bootstrap.Modal.getOrCreateInstance(document.getElementById('addItemModal')).hide();            this.#resetModal();        });        document.getElementById('addItemModal')?.addEventListener('show.bs.modal', () => {            if (this.#state.warehouse) {                document.getElementById('modalWarehouseId').disabled = true;                document.getElementById('modalWarehouseId').value = this.#state.warehouse;            }        });        document.getElementById('addItemModal')?.addEventListener('hidden.bs.modal', () => this.#resetModal());    }    #bindTableEvents() {        document.getElementById('itemsTableBody')?.addEventListener('click', (e) => {            const btn = e.target.closest('.btn-remove-item');            if (!btn) return;            btn.closest('tr').remove();            this.#updateNoItemsVisibility();            this.#reindexTableRows();        });    }    #updateNoItemsVisibility() {        const tbody = document.getElementById('itemsTableBody');        const msg = document.getElementById('noItemsMessage');        if (!tbody || !msg) return;        msg.style.display = tbody.querySelectorAll('tr').length > 0 ? 'none' : 'block';    }    #reindexTableRows() {        const tbody = document.getElementById('itemsTableBody');        if (!tbody) return;        // Láº¥y táº¥t cáº£ cÃ¡c tháº» <tr> Ä‘ang cÃ³ trong báº£ng        const rows = tbody.querySelectorAll('tr');        // Láº·p qua tá»«ng dÃ²ng vÃ  Ä‘Ã¡nh láº¡i index tá»« 0, 1, 2...        rows.forEach((row, newIndex) => {            // 1. Cáº­p nháº­t láº¡i ID cá»§a dÃ²ng (tuá»³ chá»n nhÆ°ng nÃªn lÃ m cho Ä‘á»“ng bá»™)            row.id = `item-row-${newIndex}`;            // 2. Cáº­p nháº­t name cho táº¥t cáº£ tháº» input, select, textarea            const inputs = row.querySelectorAll('input, select, textarea');            inputs.forEach(input => {                if (input.name) {                    // TÃ¬m máº«u "Items[sá»‘_báº¥t_ká»³]" vÃ  thay báº±ng "Items[newIndex]"                    input.name = input.name.replace(/Items\[\d+\]/, `Items[${newIndex}]`);                }            });            // 3. Cáº­p nháº­t data-valmsg-for cho cÃ¡c tháº» span hiá»ƒn thá»‹ lá»—i validation cá»§a ASP.NET            const validationSpans = row.querySelectorAll('.field-validation-valid, .field-validation-error');            validationSpans.forEach(span => {                const valmsgFor = span.getAttribute('data-valmsg-for');                if (valmsgFor) {                    span.setAttribute(                        'data-valmsg-for',                        valmsgFor.replace(/Items\[\d+\]/, `Items[${newIndex}]`)                    );                }            });        });        const form = document.getElementById('#createGoodsReceiptForm');        $(form).removeData('validator').removeData('unobtrusiveValidation');        $.validator.unobtrusive.parse(form);    }    #addOrIncrementItem(product) {        const tbody = document.getElementById('itemsTableBody');        if (!tbody) return;        const existingRow = Array.from(tbody.querySelectorAll('tr')).find(row => {            const idInput = row.querySelector('.item-product-id');            return idInput && idInput.value === product.id;        });        if (existingRow) {            const qtyInput = existingRow.querySelector('.item-qty');            if (qtyInput) {                const current = parseFloat(DecimalFields.stripFormatting(qtyInput.value, 2)) || 0;                const newQty = current + 1;                qtyInput.value = DecimalFields.formatQuantity                    ? DecimalFields.formatQuantity(newQty) : newQty;                qtyInput.dispatchEvent(new Event('change', { bubbles: true }));            }            existingRow.classList.add('table-success');            setTimeout(() => existingRow.classList.remove('table-success'), 700);            return;        }        this.#addItemToTable(product, 1, null, this.#state.warehouse);        const newRow = tbody.lastElementChild;        if (newRow) {            newRow.classList.add('table-success');            setTimeout(() => newRow.classList.remove('table-success'), 700);        }    }    #addItemToTable(product, quantity, unitCost, warehouseId = '') {        const tbody = document.getElementById('itemsTableBody');        if (!tbody) return;        const existingInputs = tbody.querySelectorAll('input[name^="Items["]');        let maxIndex = -1;        existingInputs.forEach(inp => {            const m = inp.name.match(/Items\[(\d+)\]/);            if (m) maxIndex = Math.max(maxIndex, parseInt(m[1]));        });        const i = maxIndex + 1;        const qtyFormatted = DecimalFields.formatQuantity ? DecimalFields.formatQuantity(quantity) : quantity;        const costFormatted = unitCost != null            ? (DecimalFields.formatCurrency ? DecimalFields.formatCurrency(unitCost) : unitCost) : '';        const pictureHtml = product.picture            ? `<img src="${product.picture}" class="rounded me-2 product-picture"                    style="width:36px;height:36px;object-fit:cover;flex-shrink:0;" alt="${escapeHtml(product.name)}" />`            : `<div class="d-flex align-items-center justify-content-center rounded bg-light me-2"                    style="width:36px;height:36px;flex-shrink:0;">                    <i class="bi bi-image text-muted small"></i>               </div>`;        const row = document.createElement('tr');        row.id = `item-row-${i}`;        row.innerHTML = `            <td class="ps-3">                <div class="d-flex align-items-center">                    ${pictureHtml}                    <div>                        <div class="fw-medium item-product-name">${escapeHtml(product.name)}</div>                    </div>                </div>                <input type="hidden" name="Items[${i}].ProductId" value="${escapeHtml(product.id)}" class="item-product-id" />            </td>            <td class="text-center">                <input name="Items[${i}].Quantity" value="${qtyFormatted}"                       class="form-control form-control-sm text-end item-qty no-additional-element"                       data-decimal="quantity" min="0.001" placeholder="0"                       data-val="true"                       data-val-required="Vui lÃ²ng nháº­p sá»‘ lÆ°á»£ng."                       data-val-range="Sá»‘ lÆ°á»£ng pháº£i lá»›n hÆ¡n 0."                       data-val-range-min="0.001"                       data-val-number="Sá»‘ lÆ°á»£ng khÃ´ng Ä‘Ãºng." />                <span class="small text-danger field-validation-valid"                      data-valmsg-for="Items[${i}].Quantity" data-valmsg-replace="true"></span>            </td>            <td>                <select id="Items[${i}]_WarehouseId" name="Items[${i}].WarehouseId" class="form-select item-warehouse" ${this.#state.warehouse ? 'disabled' : ''} data-val="${!this.#warehouseSettings.AllowNonWarehouse}" data-required="Vui lÃ²ng chá»n kho hÃ ng">                    ${this.#warehouseSettings.AllowNonWarehouse ? `<option value="" ${warehouseId ? '' : 'selected'}>(KhÃ´ng chá»n)</option>` : ''}                    ${this.#warehouseOptions.map(option => `<option value="${option.value}" ${option.value == warehouseId ? 'selected' : ''}>${option.label}</option>`).join('')}                </select>                <span class="small text-danger field-validation-valid"                      data-valmsg-for="Items[${i}].WarehouseId" data-valmsg-replace="true"></span>            </td>            <td class="text-end">                <input name="Items[${i}].UnitCost" value="${costFormatted}"                       class="form-control form-control-sm text-end item-unit-cost no-additional-element no-hint"                       data-decimal="currency" placeholder="(KhÃ´ng rÃµ)" min="0"                       data-val-range="ÄÆ¡n giÃ¡ khÃ´ng Ä‘Æ°á»£c Ã¢m."                       data-val-range-min="0"                       data-val-number="ÄÆ¡n giÃ¡ khÃ´ng Ä‘Ãºng." />                <span class="small text-danger field-validation-valid"                      data-valmsg-for="Items[${i}].UnitCost" data-valmsg-replace="true"></span>            </td>            <td class="text-end pe-3">                <button type="button" class="btn-table-action danger border-0 bg-transparent shadow-none btn-remove-item">                    <i class="bi bi-trash"></i>                </button>            </td>`;        tbody.appendChild(row);        DecimalFields.autoWrap?.(row);        this.#updateNoItemsVisibility();        const form = document.getElementById('#createGoodsReceiptForm');        $(form).removeData('validator').removeData('unobtrusiveValidation');        $.validator.unobtrusive.parse(form);    }    #resetModal() {        document.getElementById('modalProductId').value = '';        if (this.#warehouseSettings.AllowNonWarehouse) {            document.getElementById('modalWarehouseId').value = '';        }        document.getElementById('modalQty').value = '1';        if (document.getElementById('modalUnitCost'))            document.getElementById('modalUnitCost').value = '';        document.getElementById('productPickerError')?.classList.add('d-none');        document.querySelector('#modalProductDetails .currency-hint')?.classList.remove('visible');        document.getElementById('modalQtyError')?.classList.add('d-none');        document.getElementById('modalUnitCostError')?.classList.add('d-none');        document.getElementById('modalWarehouseError')?.classList.add('d-none');        document.getElementById('modalProductDetails')?.classList.add('d-none');        document.getElementById('btnAddItemConfirm')?.classList.add('d-none');        this.#productPicker?.clear();    }}// â”€â”€â”€ Helpers â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€function escapeHtml(str) {    return String(str ?? '')        .replace(/&/g, '&amp;')        .replace(/</g, '&lt;')        .replace(/>/g, '&gt;')        .replace(/"/g, '&quot;');}