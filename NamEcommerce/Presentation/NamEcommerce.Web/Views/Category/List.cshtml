@model CategoryListModel
@{
    ViewData[ViewConstants.PageTitle] = "Danh mục";
    string? query = Model.Keywords;
}

<div class="page-header">
    <div>
        <h1 class="page-header-title">Danh mục</h1>
        <p class="page-header-subtitle">Quản lý danh mục phân loại hàng hóa</p>
    </div>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-lg me-1"></i>Thêm mới
    </a>
</div>

<div class="content-card" data-responsive-list data-list-key="category-list">
    <div class="action-toolbar">
        <div class="search-group">
            <partial name="_Search" model="@(new CategoryListSearchModel { Keywords = Model.Keywords })" />
        </div>
        <div class="responsive-list-toolbar" role="group" aria-label="Chọn kiểu hiển thị">
            <button type="button" class="responsive-list-toggle" data-list-view="list" title="Dạng danh sách" aria-label="Dạng danh sách" aria-pressed="false">
                <i class="bi bi-list-ul"></i>
            </button>
            <button type="button" class="responsive-list-toggle" data-list-view="grid" title="Dạng lưới" aria-label="Dạng lưới" aria-pressed="false">
                <i class="bi bi-grid-3x3-gap"></i>
            </button>
        </div>
    </div>

    <div class="responsive-list-panel" data-list-view-panel="list">
        <div class="table-responsive">
        <table class="table data-table">
            <thead>
                <tr>
                    <th class="ps-4">Tên danh mục</th>
                    <th>Danh mục cha</th>
                    <th style="width: 100px;"></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Data.Pagination.TotalCount > 0)
                {
                    @foreach (var item in Model.Data)
                    {
                        <tr>
                            <td class="ps-4">
                                <span class="fw-medium">
                                    @HighlighHelper.Highlight(item.Name, query)
                                </span>
                            </td>
                            <td class="text-muted">
                                @item.Breadcrumb
                            </td>
                            <td class="text-end pe-3">
                                <a class="btn-table-action me-1" asp-action="Edit" asp-route-id="@item.Id" title="Chỉnh sửa">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <button type="button" class="btn-table-action danger btnDelete"
                                        data-id="@item.Id" data-name="@item.Name" title="Xóa">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3">
                            <div class="empty-state">
                                <div class="empty-icon"><i class="bi bi-grid"></i></div>
                                <h5>Chưa có danh mục nào</h5>
                                <p>Tạo danh mục đầu tiên để phân loại hàng hóa.</p>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        </div>
    </div>

    <div class="responsive-list-panel responsive-list-grid" data-list-view-panel="grid" hidden>
        @if (Model.Data.Pagination.TotalCount > 0)
        {
            @foreach (var item in Model.Data)
            {
                <article class="list-card">
                    <div class="list-card-header">
                        <div class="list-card-title-group">
                            <div class="list-card-title">@HighlighHelper.Highlight(item.Name, query)</div>
                            <div class="list-card-meta">@item.Breadcrumb</div>
                        </div>
                    </div>
                    <div class="list-card-actions">
                        <a class="btn-table-action" asp-action="Edit" asp-route-id="@item.Id" title="Chỉnh sửa">
                            <i class="bi bi-pencil"></i>
                        </a>
                        <button type="button" class="btn-table-action danger btnDelete"
                                data-id="@item.Id" data-name="@item.Name" title="Xóa">
                            <i class="bi bi-trash"></i>
                        </button>
                    </div>
                </article>
            }
        }
        else
        {
            <div class="empty-state">
                <div class="empty-icon"><i class="bi bi-grid"></i></div>
                <h5>Chưa có danh mục nào</h5>
                <p>Tạo danh mục đầu tiên để phân loại hàng hóa.</p>
            </div>
        }
    </div>

    @if (Model.Data.Pagination.TotalCount > 0)
    {
        <partial name="_Pager" model="Model.Data.Pagination" />
    }
</div>

@foreach (var item in Model.Data)
{
    <partial name="_DeleteForm" model="@item.Id" />
}

@section Scripts {
    <script type="module" defer>
        import { confirm } from "/modules/modals.js";
        (function () {
        $('.btnDelete').on('click', async function (e) {
        e.preventDefault();
        const name = $(this).data('name');
        $(this).prop('disabled', true);
        const confirmed = await confirm('Xóa danh mục', `Bạn muốn xóa danh mục <strong>${name}</strong>?`);
        if (!confirmed) { $(this).prop('disabled', false); return; }
        $(`#deleteForm${$(this).data('id')}`).submit();
        });
        })();
    </script>
}
