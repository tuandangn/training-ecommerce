@model StatusBadgeModel

<span class="ui-status-badge ui-status-badge--@Model.Tone">
    @if (!string.IsNullOrWhiteSpace(Model.Icon))
    {
        <i class="bi @Model.Icon" aria-hidden="true"></i>
    }
    <span>@Model.Text</span>
</span>
