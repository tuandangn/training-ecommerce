@model PurchaseOrderDetailsModel
@{
    ViewData[ViewConstants.PageTitle] = $"Đơn nhập hàng - {Model.Info.Code}";
    
    var (badgeClass, statusLabel, icon) = (PurchaseOrderStatus)Model.Info.Status switch
    {
        PurchaseOrderStatus.Draft => ("secondary", "Bản nháp", "bi-pencil"),
        PurchaseOrderStatus.Submitted => ("primary", "Đã gửi", "bi-send"),
        PurchaseOrderStatus.Approved => ("info", "Đã duyệt", "bi-check-all"),
        PurchaseOrderStatus.Receiving => ("warning", "Đang nhận hàng", "bi-box-arrow-in-down"),
        PurchaseOrderStatus.Completed => ("success", "Hoàn tất", "bi-check-circle-fill"),
        PurchaseOrderStatus.Cancelled => ("danger", "Đã hủy", "bi-x-circle"),
        _ => ("secondary", "Không rõ", "bi-question-circle")
    };
    
    var successMessage = TempData[ViewConstants.PurchaseOrderSuccessMessage] as string;
    var errorMessage = TempData[ViewConstants.PurchaseOrderErrorMessage] as string;
}

<div class="page-header">
    <div>
        <h1 class="page-header-title d-flex align-items-center gap-2">
            @Model.Info.Code
            <span class="status-badge @badgeClass"><i class="bi @icon"></i>@statusLabel</span>
        </h1>
        <p class="page-header-subtitle">
            <a asp-action="List" class="text-decoration-none text-muted"><i class="bi bi-arrow-left me-1"></i>Trở về danh sách</a>
        </p>
    </div>
    <div class="d-flex gap-2">
        @if (Model.Info.Status == (int)PurchaseOrderStatus.Draft)
        {
            <form asp-action="SubmitsPurchaseOrder" asp-route-id="@Model.Info.Id" method="post" class="d-inline">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-send me-1"></i> Gửi đơn nhập
                </button>
            </form>
        }
        else if (Model.Info.Status == (int)PurchaseOrderStatus.Submitted)
        {
            <form asp-action="ChangeStatus" asp-route-id="@Model.Info.Id" asp-route-status="@((int)PurchaseOrderStatus.Approved)" method="post" class="d-inline">
                <button type="submit" class="btn btn-info text-white">
                    <i class="bi bi-check-all me-1"></i> Duyệt đơn
                </button>
            </form>
        }
    </div>
</div>

@if (!string.IsNullOrEmpty(successMessage))
{
    <div class="alert alert-success alert-dismissible fade show alert-global mb-3" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i>@successMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (!string.IsNullOrEmpty(errorMessage))
{
    <div class="alert alert-danger alert-dismissible fade show alert-global mb-3" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>@errorMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="row g-3">
    <div class="col-lg-8">
        <div class="content-card mb-3">
            <div class="content-card-header">
                <h5>
                    <i class="bi bi-box-seam me-2 text-primary"></i>
                    Danh sách hàng hóa
                    <span class="status-badge secondary">@Model.Info.Items.Count hàng hóa</span>
                </h5>
                <div class="d-flex align-items-center gap-2">
                    @if (Model.Info.CanAddItems)
                    {
                        <button type="button" class="btn btn-sm btn-outline-primary" data-bs-toggle="modal"
                                data-bs-target="#addProductModal">
                            <i class="bi bi-plus-lg me-1"></i>Thêm
                        </button>
                    }
                </div>
            </div>
            <div class="table-responsive">
                <table class="table data-table mb-0">
                    <thead>
                        <tr>
                            <th class="ps-3">Hàng hóa</th>
                            <th class="text-center" style="width: 12%">SL Đặt</th>
                            <th class="text-center" style="width: 12%">Đã Nhận</th>
                            <th class="text-end" style="width: 18%">Đơn giá</th>
                            <th class="text-end d-none d-md-table-cell @(Model.Info.CanAddItems ? "" : "pe-3")" style="width: 18%">Thành tiền</th>
                            @if (Model.Info.CanAddItems || Model.Info.CanReceiveGoods)
                            {
                                <th class="text-end pe-2" style="width: 48px"></th>
                            }
                        </tr>
                    </thead>
                    <tbody id="itemsTableBody">
                        @if (Model.Info.Items.Count > 0)
                        {
                            @foreach (var item in Model.Info.Items)
                            {
                                <tr data-item-id="@item.Id">
                                    <td class="ps-4">
                                        <div class="d-flex align-items-center gap-2">
                                            @if (!string.IsNullOrEmpty(item.ProductPicture))
                                            {
                                                <img src="@item.ProductPicture" class="rounded d-none d-lg-block"
                                                     style="width: 36px; height: 36px; object-fit: cover;" alt="" />
                                            }
                                            else
                                            {
                                                <div class="d-none d-lg-flex align-items-center justify-content-center rounded bg-light"
                                                     style="width: 36px; height: 36px; flex-shrink: 0;">
                                                    <i class="bi bi-image text-muted small"></i>
                                                </div>
                                            }
                                            <span class="fw-medium text-dark">@item.ProductName</span>
                                        </div>
                                    </td>
                                    <td class="text-center fw-600">
                                        <span class="quantity-text">@item.QuantityOrdered.ToString(ViewConstants.NumberCustomFormat)</span>
                                    </td>
                                    <td class="text-center">
                                        @if (item.QuantityReceived >= item.QuantityOrdered)
                                        {
                                            <span class="status-badge success py-1 px-2">
                                                <i class="bi bi-check-circle-fill"></i> @item.QuantityReceived.ToString(ViewConstants.NumberCustomFormat)
                                            </span>
                                        }
                                        else if (item.QuantityReceived > 0)
                                        {
                                            <span class="status-badge warning py-1 px-2">
                                                @item.QuantityReceived.ToString(ViewConstants.NumberCustomFormat)
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="text-muted fst-italic small">Chưa nhận</span>
                                        }
                                    </td>
                                    <td class="text-end text-muted">
                                        <span class="unit-price-text">
                                            @item.UnitCost.ToString(ViewConstants.CurrencyDisplayFormat)
                                        </span>
                                    </td>
                                    <td class="text-end fw-600 text-primary d-none d-md-table-cell @(Model.Info.CanAddItems ? "" : "pe-4")">
                                        <span class="total-price-text">
                                            @item.TotalCost.ToString(ViewConstants.CurrencyDisplayFormat)
                                        </span>
                                    </td>
                                    @if (Model.Info.CanAddItems || Model.Info.CanReceiveGoods)
                                    {
                                        <td class="text-end pe-4">
                                            <div class="dropdown">
                                                <button class="btn-table-action" type="button" data-bs-toggle="dropdown">
                                                    <i class="bi bi-three-dots-vertical"></i>
                                                </button>
                                                <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
                                                    @if (Model.Info.CanReceiveGoods)
                                                    {
                                                        <li>
                                                            <button class="dropdown-item" data-bs-toggle="modal" data-bs-target="#receiveItemModal"
                                                                data-item-id="@item.Id" data-item-name="@item.ProductName" data-item-remaining="@item.RemainingQuantity">
                                                                <i class="bi bi-truck"></i>
                                                                Nhận hàng
                                                            </button>
                                                        </li>
                                                    }
                                                    @if (Model.Info.CanAddItems)
                                                    {
                                                        <li>
                                                            <button class="dropdown-item btnRemoveItem" data-id="@item.Id"
                                                                    disabled="@(Model.Info.Items.Count <= 1)"
                                                                    data-purchaseorder="@Model.Info.Id" data-name="@item.ProductName">
                                                                <i class="bi bi-trash me-2 text-danger"></i>
                                                                Xóa bỏ
                                                            </button>
                                                        </li>
                                                    }
                                                </ul>
                                            </div>
                                        </td>
                                    }

                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="@(Model.Info.CanAddItems ? 6 : 5)">
                                    <div class="py-4 text-center text-muted">
                                        <i class="bi bi-box-seam me-1"></i> Chưa có sản phẩm nào.
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <div class="@(Model.Info.Items.Count > 0 ? "" : "d-none") order-summary" id="tableFooter">
                <div class="order-summary-row">
                    <span class="label">Tạm tính</span>
                    <span class="value" id="subTotal">
                        @((Model.Info.TotalAmount - Model.Info.TaxAmount - Model.Info.ShippingAmount).ToString(ViewConstants.CurrencyDisplayFormat))
                    </span>
                </div>
                <div class="order-summary-row">
                    <span class="label">Phí vận chuyển</span>
                    <span class="value text-muted">
                        + @Model.Info.ShippingAmount.ToString(ViewConstants.CurrencyDisplayFormat)
                    </span>
                </div>
                <div class="order-summary-row">
                    <span class="label">Thuế VAT</span>
                    <span class="value text-muted">
                        + @Model.Info.TaxAmount.ToString(ViewConstants.CurrencyDisplayFormat)
                    </span>
                </div>
                <div class="order-summary-row total">
                    <span class="label">Tổng cộng</span>
                    <span class="value" id="grandTotal">
                        @Model.Info.TotalAmount.ToString(ViewConstants.CurrencyDisplayFormat)
                    </span>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-4">
        @if (Model.CanModifyInfo && Model.ModifyInfo != null)
        {
            <div class="content-card mb-3">
                <div class="content-card-header">
                    <h5>
                        <i class="bi bi-pencil-square me-2 text-primary"></i>
                        Chỉnh sửa thông tin
                    </h5>
                </div>
                <div class="content-card-body">
                    <partial name="Details.InfoModify" model="Model.ModifyInfo" />
                </div>
            </div>
        }
        else
        {
            <!-- Thông tin chung -->
            <div class="content-card mb-3">
                <div class="content-card-header">
                    <h5>
                        <i class="bi bi-info-circle me-2 text-primary"></i>
                        Thông tin chung
                    </h5>
                </div>
                <div class="content-card-body">
                    <div class="mb-3">
                        <label class="form-label small fw-bold text-muted text-uppercase d-block mb-2">Nhà cung cấp</label>
                        <div class="d-flex align-items-center">
                            <div class="bg-light rounded-circle me-3 d-flex align-items-center justify-content-center" style="width: 48px; height: 48px;">
                                <i class="bi bi-truck text-primary fs-4"></i>
                            </div>
                            <div>
                                <div class="fw-bold text-dark fs-6">@(Model.Info.VendorName ?? "N/A")</div>
                                <div class="text-muted small">Mã đơn: @Model.Info.Code</div>
                            </div>
                        </div>
                    </div>
                    <div class="">
                        <label class="form-label small fw-bold text-muted text-uppercase d-block mb-2">Kho nhập hàng</label>
                        <div class="d-flex align-items-center">
                            <div class="bg-light rounded-circle me-3 d-flex align-items-center justify-content-center" style="width: 48px; height: 48px;">
                                <i class="bi bi-geo-alt text-info fs-4"></i>
                            </div>
                            <div>
                                <div class="fw-bold text-dark">@(Model.Info.WarehouseName ?? "N/A")</div>
                                <div class="text-muted small fst-italic">Ngày tạo: @Model.Info.CreatedOn.ToString("dd/MM/yyyy")</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Tóm tắt tài chính -->
            <div class="content-card mb-3">
                <div class="content-card-header">
                    <h5>
                        <i class="bi bi-wallet2 me-2 text-primary"></i>
                        Tổng cộng
                    </h5>
                </div>
                <div class="content-card-body">
                    <div class="d-flex justify-content-between mb-2">
                        <span class="text-muted">Tổng tiền hàng:</span>
                        <span class="fw-bold">@((Model.Info.TotalAmount - Model.Info.TaxAmount - Model.Info.ShippingAmount).ToString(ViewConstants.CurrencyDisplayFormat))</span>
                    </div>
                    <div class="d-flex justify-content-between mb-2">
                        <span class="text-muted">Phí vận chuyển:</span>
                        <span class="fw-bold">+ @Model.Info.ShippingAmount.ToString(ViewConstants.CurrencyDisplayFormat)</span>
                    </div>
                    <div class="d-flex justify-content-between mb-2">
                        <span class="text-muted">Thuế VAT:</span>
                        <span class="fw-bold">+ @Model.Info.TaxAmount.ToString(ViewConstants.CurrencyDisplayFormat)</span>
                    </div>
                    <hr class="border-light my-3">
                    <div class="d-flex justify-content-between align-items-end">
                        <span class="text-dark fw-bold">TỔNG CỘNG:</span>
                        <span class="text-primary fw-bold fs-5">@Model.Info.TotalAmount.ToString(ViewConstants.CurrencyDisplayFormat)</span>
                    </div>
                </div>
            </div>
        }
        @if (Model.Info.Status != (int)PurchaseOrderStatus.Completed && Model.Info.Status != (int)PurchaseOrderStatus.Cancelled)
        {
            <div class="content-card p-3">
                <form asp-action="CancelPurchaseOrder" asp-route-id="@Model.Info.Id" method="post">
                    <button type="submit" class="btn btn-outline-danger w-100" onclick="return confirm('Bạn có chắc chắn muốn hủy đơn hàng này?')">
                        <i class="bi bi-x-circle me-1"></i> Hủy bỏ đơn nhập
                    </button>
                </form>
            </div>
        }
    </div>
</div>

<!-- Add Product Modal -->
@if (Model.Info.CanAddItems)
{
    <div class="modal fade" id="addProductModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow rounded-4">
                <div class="modal-header border-0 pb-0">
                    <h5 class="modal-title fw-bold">Thêm sản phẩm</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form action="#" method="post" id="addProductForm">
                    <input type="hidden" name="purchaseOrderId" value="@Model.Info.Id" />
                    <div class="modal-body py-3">
                        <div class="mb-3">
                            <div id="productPicker"></div>
                            <input type="text" class="visually-hidden" id="addProductId" name="ProductId" value=""
                                   data-val="true" data-val-required="Vui lòng chọn hàng hóa." />
                            <span class="small text-danger field-validation-valid" data-valmsg-for="ProductId"
                                  data-valmsg-replace="true"></span>
                        </div>
                        <div id="modalProductInfo" class="d-none">
                            <div class="row g-2">
                                <div class="col-6">
                                    <label class="form-label small fw-medium text-muted" for="addQuantity">Số lượng</label>
                                    <input type="number" id="addQuantity" name="quantity" class="form-control"
                                           value="1" min="0.01" step="any" data-val="true"
                                           data-val-required="Vui lòng nhập số lượng." />
                                    <span class="small text-danger field-validation-valid"
                                          data-valmsg-for="quantity" data-valmsg-replace="true"></span>
                                </div>
                                <div class="col-6">
                                    <label class="form-label small fw-medium text-muted noRefill" for="addUnitPrice">
                                        Đơn giá
                                    </label>
                                    <input type="number" id="addUnitCost" name="unitCost" class="form-control"
                                           step="any" min="0" data-val="true" data-val-required="Vui lòng nhập đơn giá." />
                                    <span class="small text-danger field-validation-valid"
                                          data-valmsg-for="unitCost" data-valmsg-replace="true"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer border-0 pt-0 pb-3">
                        <button type="button" class="btn btn-light" data-bs-dismiss="modal">Hủy</button>
                        <button type="submit" id="btnAddItem" class="btn btn-success d-none noDisabled">
                            <i class="bi bi-plus-lg me-1"></i>Thêm vào
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
}

<!-- Modal Nhập hàng -->
@if (Model.Info.CanReceiveGoods)
{
    <div class="modal fade" id="receiveItemModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow rounded-4">
                <div class="modal-header border-0 pb-0">
                    <h5 class="modal-title fw-bold">Nhập hàng vào kho</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form asp-action="Receive" method="post">
                    <div class="modal-body py-4">
                        <input type="hidden" name="PurchaseOrderId" value="@Model.Info.Id" />
                        <input type="hidden" name="PurchaseOrderItemId" id="modalItemId" />
                        
                        <div class="alert alert-info border-0 mb-4">
                            <div class="small fw-bold">
                                Sản phẩm:
                                <span id="modalItemName" class="fs-6 fw-bold text-dark">Tên sản phẩm</span>
                            </div>
                            <div class="small mt-1 mt-1 fst-italic">
                                Còn lại cần nhập: 
                                <span id="modalItemRemaining" class="fw-bold">0</span>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label small fw-bold text-muted text-uppercase">Số lượng nhận thực tế</label>
                            <input type="number" name="ReceivedQuantity" id="modalReceivedQty" class="form-control" value="0.0" min="0.01" step="any" required />
                        </div>

                        <div class="mb-3">
                            <label class="form-label small fw-bold text-muted text-uppercase">Kho nhập hàng</label>
                            <select name="WarehouseId" class="form-select" required>
                                @foreach (var option in Model.AvailableWarehouses.Options)
                                {
                                    <option value="@option.Id" selected="@(Model.Info.WarehouseId == option.Id)">@option.Name</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div class="modal-footer border-0 pt-0 pb-4">
                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Đóng</button>
                        <button type="submit" class="btn btn-success">
                            Xác nhận nhập kho
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script type="module">
        import ProductPicker from '/modules/ProductPicker.js';

        let productPickerInstance = null;

        document.addEventListener('DOMContentLoaded', function() {
        // Initialize Product Picker for Add Product Modal
        const addProductModal = document.getElementById('addProductModal');
        if (addProductModal) {
        initializeProductPicker();

        addProductModal.addEventListener('hidden.bs.modal', function() {
        // Reset form when modal closes
        resetAddProductForm();
        });
        }

        // Handle Add Product Form Submission
        const addProductForm = document.getElementById('addProductForm');
        if (addProductForm) {
        addProductForm.addEventListener('submit', async function(e) {
        e.preventDefault();
        if(!$(this).valid())
        return;
        await submitAddProductForm();
        });
        }

        // Initialize Receive Item Modal
        const receiveItemModal = document.getElementById('receiveItemModal');
        if (receiveItemModal) {
        receiveItemModal.addEventListener('show.bs.modal', function(event) {
        const button = event.relatedTarget;
        const itemId = button.getAttribute('data-item-id');
        const itemName = button.getAttribute('data-item-name');
        const remaining = button.getAttribute('data-item-remaining');

        document.getElementById('modalItemId').value = itemId;
        document.getElementById('modalItemName').innerText = itemName;
        document.getElementById('modalItemRemaining').innerText = remaining;
        document.getElementById('modalReceivedQty').value = remaining;
        document.getElementById('modalReceivedQty').max = remaining;
        });
        }

        // Handle Remove Item Button
        document.querySelectorAll('.btnRemoveItem').forEach(btn => {
        btn.addEventListener('click', async function(e) {
        e.preventDefault();
        const itemId = this.getAttribute('data-id');
        const purchaseOrderId = this.getAttribute('data-purchaseorder');
        const itemName = this.getAttribute('data-name');

        if (confirm(`Bạn có chắc chắn muốn xóa "${itemName}" khỏi đơn hàng?`)) {
        await removeItem(itemId, purchaseOrderId);
        }
        });
        });
        });

        function initializeProductPicker() {
        const container = document.getElementById('productPicker');
        if (container && !productPickerInstance) {
        productPickerInstance = new ProductPicker(container);
        container.addEventListener('select', (e) => {
        onProductSelected(e.detail.product);
        });
        container.addEventListener('remove', onProductRemove);
        }
        }

        function onProductSelected(product) {
        const modalProductInfo = document.getElementById('modalProductInfo');
        const addUnitCost = document.getElementById('addUnitCost');
        const addProductId = document.getElementById('addProductId');
        const btnAddProduct = document.getElementById('btnAddItem');

        console.log(product);
        modalProductInfo.classList.remove('d-none');
        addUnitCost.value = 0;
        addProductId.value = product.id;
        btnAddProduct.classList.remove('d-none');
        }
        function onProductRemove() {
        const modalProductInfo = document.getElementById('modalProductInfo');
        const addUnitCost = document.getElementById('addUnitCost');
        const addProductId = document.getElementById('addProductId');
        const btnAddProduct = document.getElementById('btnAddItem');

        modalProductInfo.classList.add('d-none');
        addUnitCost.value = 0;
        addProductId.value = '';
        btnAddProduct.classList.add('d-none');
        }

        async function submitAddProductForm() {
        const form = document.getElementById('addProductForm');

        try {
        const response = await fetch('@Url.Action("AddPurchaseOrderItem")', {
        method: 'POST',
        headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        },
        body: $(form).serialize()
        });

        const data = await response.json();

        if (data.success) {
        // Close modal and refresh page
        bootstrap.Modal.getInstance(document.getElementById('addProductModal'))?.hide();
        setTimeout(() => location.reload(), 300);
        } else {
        alert('Lỗi: ' + (data.errorMessage || 'Không thể thêm sản phẩm'));
        }
        } catch (error) {
        console.error('Error:', error);
        alert('Có lỗi xảy ra khi thêm sản phẩm');
        }
        }

        function resetAddProductForm() {
        document.getElementById('itemQuantity').value = '1';
        document.getElementById('itemUnitPrice').value = '';
        document.getElementById('modalProductInfo').classList.add('d-none');

        if (productPickerInstance) {
        productPickerInstance.clear();
        }
        }

        async function removeItem(itemId, purchaseOrderId) {
        try {
        const response = await fetch('@Url.Action("RemovePurchaseOrderItem")', {
        method: 'POST',
        headers: {
        'Content-Type': 'application/json',
        'RequestVerificationToken': document.querySelector('[name="__RequestVerificationToken"]')?.value || ''
        },
        body: JSON.stringify({
        purchaseOrderItemId: itemId,
        purchaseOrderId: purchaseOrderId
        })
        });

        const data = await response.json();

        if (data.success) {
        // Remove row from table
        document.querySelector(`tr[data-item-id="${itemId}"]`)?.remove();

        // Refresh page after short delay
        setTimeout(() => location.reload(), 300);
        } else {
        alert('Lỗi: ' + (data.errorMessage || 'Không thể xóa sản phẩm'));
        }
        } catch (error) {
        console.error('Error:', error);
        alert('Có lỗi xảy ra khi xóa sản phẩm');
        }
        }
    </script>
}
