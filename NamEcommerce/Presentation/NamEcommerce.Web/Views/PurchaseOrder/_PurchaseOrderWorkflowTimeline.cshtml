@model PurchaseOrderDetailsModel

<div class="content-card">
    <div class="content-card-header">
        <h5>Timeline</h5>
        <span class="status-badge @Model.Workflow.ReceivingStatusClass">@Model.Workflow.ReceivingStatusText</span>
    </div>
    <div class="content-card-body">
        @if (Model.Timeline.Count == 0)
        {
            <div class="workflow-soft-empty">
                <i class="bi bi-clock-history"></i>
                <span>Chưa có hoạt động.</span>
            </div>
        }
        else
        {
            <ol class="workflow-timeline-list">
                @foreach (var item in Model.Timeline)
                {
                    <li class="workflow-timeline-item @item.Tone">
                        <div class="workflow-timeline-icon">
                            <i class="bi @item.Icon"></i>
                        </div>
                        <div class="workflow-timeline-content">
                            <time>@item.OccurredOn.ToString(ViewConstants.DateTimeFormat)</time>
                            <strong>@item.Title</strong>
                            @if (!string.IsNullOrWhiteSpace(item.Description))
                            {
                                <span>@item.Description</span>
                            }
                        </div>
                    </li>
                }
            </ol>
        }
    </div>
</div>
